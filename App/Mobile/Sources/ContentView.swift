import SwiftUI
import CartModule


struct ContentView: View {
    var body: some View {
        VStack {
            Text("this is the main app")
            Text("appName")
            CartView()
        }
        
    }
}
